# Keep host build output out of the build context: a locally compiled target/ can
# otherwise be copied into the image and shadow what the build stage produces.
target/
.mvn/wrapper/maven-wrapper.jar
*.log

.git/
.gitignore
.idea/
*.iml
.DS_Store
